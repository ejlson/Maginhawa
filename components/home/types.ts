export type HeroInsets = {
  /** space reserved at the top for the navbar (px) */
  top: number;
  /** left/right margin of the hero video panel (px) */
  side: number;
  /** space reserved at the bottom for the giant wordmark (px) */
  bottom: number;
  /** corner radius of the panel (px) */
  radius: number;
};
